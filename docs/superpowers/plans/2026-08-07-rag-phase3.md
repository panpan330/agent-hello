# RAG 阶段 3（知识库运营闭环）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为知识库提供多 collection 管理、文档 CRUD（上传/编辑/删除/单独同步）、Java 元数据持久化与前端运营界面，并补全评测趋势图 RAG 检索质量线。

**Architecture:** Python 侧负责文件落盘 + Qdrant 同步（`data/knowledge_base/` 目录 + kb_* collection），Java 侧新增 `/internal/knowledge-documents` 写接口持久化 `knowledge_documents` 元数据（仿 `InternalOrderController` 五头鉴权模式）；Python 经新 `KnowledgeDocumentClient`（仿 `JavaOrderClient`）调 Java 内部接口。QdrantVectorStore 扩展 `from_settings(..., collection_name=)` 支持多库；旧库 `learning_rag_chunks_v4_1024` 保留只读兼容。前端 KnowledgeBaseView 增加上传/编辑/删除/单独同步操作，EvaluationView 趋势图增加 RAG 检索质量第三条线。

**Tech Stack:** Python 3.12 / FastAPI / httpx / Qdrant；Java 21 / Spring Boot / MyBatis；Vue 3 / Element Plus / echarts。

## Global Constraints

- Python 测试基线 **1554 passed**（不得回归）；Java `mvn test -q` 基线 **80 通过**；前端 `npm run build` 必须通过。
- 自动测试不调用真实模型、不命中真实 Embedding/Rerank API、不写入真实业务数据、不依赖真实 Redis、不连接真实 Collector/LangSmith。
- 提交信息格式：`feat:` / `fix:` / `test:` / `docs:` + 简短描述（英文）。
- 命名：Python snake_case、Java camelCase、前端沿用现有组件风格。
- `langsmith_tracing.py` / `otel_tracing.py` 保留不动。
- 用户决策：**新建 kb_* collection（4 个）+ 旧库只读兼容**；**Python 仿 java_order_client 模式调 Java 内部接口**。

---

### Task 1: Python QdrantVectorStore 多 collection 支持

**Files:**
- Modify: `projects/ai-service/app/rag/vector_store.py`（`from_settings` 加 `collection_name` 参数；新增 `list_collections()`、`count_points()`）
- Test: `projects/ai-service/tests/test_rag_vector_store.py`（追加 2 个测试）

**Interfaces:**
- Consumes: `Settings`（`resolved_qdrant_base_url` / `qdrant_collection_name` / `qdrant_timeout_seconds` / `qdrant_api_key`）
- Produces:
  - `QdrantVectorStore.from_settings(settings, *, collection_name: str | None = None) -> QdrantVectorStore`（`None` 时用 `settings.qdrant_collection_name`）
  - `QdrantVectorStore.list_collections() -> list[str]`（GET `/collections` → `result.collections[].name`）
  - `QdrantVectorStore.count_points() -> int`（GET `/collections/{name}` → `result.points_count`；404 返回 0）
  - `QdrantVectorStore.collection_name: str`（现有属性）

- [ ] **Step 1: 写失败测试**

```python
def test_from_settings_accepts_collection_name_override() -> None:
    import httpx
    store = QdrantVectorStore.from_settings(
        make_settings(), collection_name="kb_customer_policy"
    )
    assert store.collection_name == "kb_customer_policy"


def test_from_settings_defaults_to_settings_collection() -> None:
    store = QdrantVectorStore.from_settings(make_settings())
    assert store.collection_name == "learning_rag_chunks_v4_1024"
```

> 在 `test_rag_vector_store.py` 顶部已有 `make_store(handler)`（不依赖 settings）。为 from_settings 测试新增 helper：

```python
def make_settings(**overrides) -> Settings:
    from app.core.config import Settings
    base = {
        "qdrant_base_url": "http://qdrant.test",
        "qdrant_collection_name": "learning_rag_chunks_v4_1024",
        "qdrant_timeout_seconds": 1.0,
        "qdrant_api_key": None,
    }
    base.update(overrides)
    return Settings(**base)
```

- [ ] **Step 2: 运行测试确认失败**

Run: `uv run pytest tests/test_rag_vector_store.py::test_from_settings_accepts_collection_name_override -v`
Expected: FAIL（`TypeError: from_settings() got an unexpected keyword argument 'collection_name'`）

- [ ] **Step 3: 实现 from_settings + list_collections + count_points**

```python
@classmethod
def from_settings(
    cls,
    settings: Settings,
    *,
    collection_name: str | None = None,
) -> "QdrantVectorStore":
    return cls(
        base_url=settings.resolved_qdrant_base_url,
        collection_name=collection_name or settings.qdrant_collection_name,
        timeout_seconds=settings.qdrant_timeout_seconds,
        api_key=settings.qdrant_api_key,
    )

def list_collections(self) -> list[str]:
    path = "/collections"
    try:
        with self._client() as client:
            response = client.get(path)
        if response.status_code == 404:
            return []
        response.raise_for_status()
        body = response.json()
        return [item["name"] for item in body.get("result", {}).get("collections", [])]
    except Exception as exc:
        raise rag_vector_store_failed(exc) from exc

def count_points(self) -> int:
    path = f"/collections/{self.collection_name}"
    try:
        with self._client() as client:
            response = client.get(path)
        if response.status_code == 404:
            return 0
        response.raise_for_status()
        body = response.json()
        return int(body.get("result", {}).get("points_count", 0))
    except Exception as exc:
        raise rag_vector_store_failed(exc) from exc
```

> `rag_vector_store_failed` 已在 vector_store.py 顶部 import（确认存在，否则从 `app.core.exceptions` import）。

- [ ] **Step 4: 追加 list/count 的 HTTP 测试**

```python
def test_list_collections_returns_names() -> None:
    import httpx

    def handler(request: httpx.Request) -> httpx.Response:
        assert request.url.path == "/collections"
        body = {
            "result": {
                "collections": [
                    {"name": "kb_customer_policy"},
                    {"name": "kb_account_security"},
                ]
            }
        }
        return httpx.Response(200, json=body)

    store = make_store(handler)
    assert store.list_collections() == ["kb_customer_policy", "kb_account_security"]


def test_count_points_returns_zero_when_collection_missing() -> None:
    import httpx

    def handler(request: httpx.Request) -> httpx.Response:
        return httpx.Response(404, json={"status": {"error": "Not found"}})

    store = make_store(handler)
    assert store.count_points() == 0
```

- [ ] **Step 5: 运行全部 vector_store 测试 + 全量**

Run: `uv run pytest tests/test_rag_vector_store.py -q` → 通过（原 22 + 新增 4 = 26）
Run: `uv run pytest -q` → **1558 passed**

- [ ] **Step 6: Commit**

```bash
git add projects/ai-service/
git commit -m "feat: support multi-collection via from_settings override and collection listing"
```

---

### Task 2: Python collection 管理端点

**Files:**
- Modify: `projects/ai-service/app/routers/knowledge_base.py`（新增 GET `/api/knowledge-base/collections`）
- Modify: `projects/ai-service/app/schemas/knowledge_base.py`（新增响应 schema）
- Modify: `projects/ai-service/app/services/rag_service.py`（新增 `build_collection_status` helper，或在 router 内联——优先 router 内联，见下）
- Test: `projects/ai-service/tests/test_knowledge_base_api.py`

**Interfaces:**
- Consumes: `default_rag_knowledge_bases()`（`knowledge_routing.py:184`，返回 `list[RagKnowledgeBaseDefinition]`，字段 `knowledge_base_id` / `collection_name` / `display_name` / `business_domains` / `permission_groups`）；`QdrantVectorStore.list_collections()` / `count_points()`（Task 1）
- Produces:
  - `GET /api/knowledge-base/collections` → `KnowledgeBaseCollectionsResponse`
  - `KnowledgeBaseCollectionStatus` schema：`collection_name: str`、`knowledge_base_ids: list[str]`、`display_name: str`、`point_count: int`、`exists: bool`、`is_legacy: bool`
  - `KnowledgeBaseCollectionsResponse`：`collections: list[KnowledgeBaseCollectionStatus]`、`legacy_collections: list[KnowledgeBaseCollectionStatus]`、`trace_id: str`

- [ ] **Step 1: 写失败测试**

```python
def test_collections_endpoint_lists_managed_and_legacy_collections(
    client, monkeypatch
) -> None:
    from app.services.knowledge_management import FakeKnowledgeStore  # 若有；否则 monkeypatch QdrantVectorStore

    class FakeStore:
        def list_collections(self):
            return ["kb_customer_policy", "kb_account_security"]

        def count_points(self):
            return 3

    monkeypatch.setattr(
        "app.routers.knowledge_base._build_vector_store",
        lambda settings, collection_name: FakeStore(),
    )
    response = client.get("/api/knowledge-base/collections")
    assert response.status_code == 200
    data = response.json()["data"]
    names = [c["collection_name"] for c in data["collections"]]
    assert "kb_customer_policy" in names
    assert all(c["exists"] for c in data["collections"])
    assert data["legacy_collections"]  # 旧库 learning_rag_chunks_v4_1024
```

> 现有 `test_knowledge_base_api.py` 的 `client` fixture 已存在（查该文件确认）。若 router 中无 `_build_vector_store` 可打补丁符号，则先写一个模块级工厂函数 `_build_vector_store(settings, *, collection_name=None)` 再 patch 它。

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_knowledge_base_api.py::test_collections_endpoint_lists_managed_and_legacy_collections -v`
Expected: FAIL（404 或 405）

- [ ] **Step 3: 实现 schema**

`app/schemas/knowledge_base.py` 追加：

```python
class KnowledgeBaseCollectionStatus(BaseModel):
    collection_name: str = Field(min_length=1)
    knowledge_base_ids: list[str] = Field(default_factory=list)
    display_name: str = ""
    point_count: int = Field(default=0, ge=0)
    exists: bool = False
    is_legacy: bool = False


class KnowledgeBaseCollectionsResponse(BaseModel):
    collections: list[KnowledgeBaseCollectionStatus] = Field(default_factory=list)
    legacy_collections: list[KnowledgeBaseCollectionStatus] = Field(default_factory=list)
    trace_id: str = Field(min_length=1)
```

- [ ] **Step 4: 实现端点**

`app/routers/knowledge_base.py`：

```python
def _build_vector_store(settings, *, collection_name: str | None = None):
    from app.rag.vector_store import QdrantVectorStore
    return QdrantVectorStore.from_settings(settings, collection_name=collection_name)


@router.get("/collections", response_model=KnowledgeBaseCollectionsResponse)
def list_collections(
    settings: Settings = Depends(get_settings),
    trace: TraceContext = Depends(get_trace_context),
) -> KnowledgeBaseCollectionsResponse:
    definitions = default_rag_knowledge_bases()
    managed: dict[str, list[RagKnowledgeBaseDefinition]] = {}
    for definition in definitions:
        managed.setdefault(definition.collection_name, []).append(definition)

    legacy_name = settings.qdrant_collection_name  # learning_rag_chunks_v4_1024
    all_names = _build_vector_store(settings).list_collections()

    result: list[KnowledgeBaseCollectionStatus] = []
    for collection_name, defs in sorted(managed.items()):
        exists = collection_name in all_names
        point_count = 0
        if exists:
            point_count = _build_vector_store(settings, collection_name=collection_name).count_points()
        result.append(
            KnowledgeBaseCollectionStatus(
                collection_name=collection_name,
                knowledge_base_ids=[d.knowledge_base_id for d in defs],
                display_name=" / ".join(d.display_name for d in defs),
                point_count=point_count,
                exists=exists,
                is_legacy=False,
            )
        )

    legacy: list[KnowledgeBaseCollectionStatus] = []
    if legacy_name and legacy_name not in managed:
        legacy.append(
            KnowledgeBaseCollectionStatus(
                collection_name=legacy_name,
                knowledge_base_ids=[],
                display_name="Legacy single collection",
                point_count=_build_vector_store(settings).count_points()
                if legacy_name in all_names else 0,
                exists=legacy_name in all_names,
                is_legacy=True,
            )
        )

    return KnowledgeBaseCollectionsResponse(
        collections=result,
        legacy_collections=legacy,
        trace_id=trace.trace_id,
    )
```

> 对照现有 `knowledge_base.py` 路由的依赖注入风格（`get_settings` / `get_trace_context` 的确切名字查该文件顶部），保持一致。

- [ ] **Step 5: 运行测试**

Run: `uv run pytest tests/test_knowledge_base_api.py -q` → 全过
Run: `uv run pytest -q` → **1560 passed**

- [ ] **Step 6: Commit**

```bash
git add projects/ai-service/
git commit -m "feat: add knowledge-base collections endpoint"
```

---

### Task 3: Java 侧 KnowledgeDocument 写接口（内部接口）

**Files:**
- Modify: `projects/java-business-service/src/main/java/com/panpan/aibusinessservice/mapper/KnowledgeDocumentMapper.java`（+3 方法）
- Modify: `projects/java-business-service/src/main/resources/mapper/KnowledgeDocumentMapper.xml`（+3 语句）
- Modify: `projects/java-business-service/src/main/java/com/panpan/aibusinessservice/service/KnowledgeDocumentService.java`（+2 方法）
- Modify: `projects/java-business-service/src/main/java/com/panpan/aibusinessservice/service/impl/KnowledgeDocumentServiceImpl.java`
- Create: `projects/java-business-service/src/main/java/com/panpan/aibusinessservice/dto/KnowledgeDocumentWriteRequest.java`
- Create: `projects/java-business-service/src/main/java/com/panpan/aibusinessservice/controller/InternalKnowledgeDocumentController.java`
- Modify: `projects/java-business-service/src/test/java/com/panpan/aibusinessservice/AuthAndKnowledgeControllerTest.java` 或新建 `InternalKnowledgeDocumentControllerTest.java`

**Interfaces:**
- Consumes: `InternalRequestResolver`（`resolve(HttpServletRequest)` → `InternalRequestContext`，含 `traceId()` / `tenantId()`）；`InternalApiTestSupport.withInternalHeaders(...)`；`ApiResponse`；`TraceHeaders`
- Produces（Java 内部接口，路径前缀 `/internal/knowledge-documents`）：
  - `POST /internal/knowledge-documents`（body：`KnowledgeDocumentWriteRequest`；幂等 upsert，`idempotencyKey` 可选）
  - `DELETE /internal/knowledge-documents/{documentId}`（按 tenant_id + document_id 删除）
  - `KnowledgeDocumentWriteRequest` 字段：`document_id` / `title` / `doc_type` / `business_domain` / `permission_group` / `status` / `source_file_name` / `chunk_count` / `updated_by`
  - Service：`KnowledgeDocumentView upsertDocument(InternalRequestContext context, KnowledgeDocumentWriteRequest request, String idempotencyKey)`、`boolean deleteDocument(InternalRequestContext context, String documentId)`
  - Mapper：`int upsert(KnowledgeDocument entity)`（INSERT ... ON DUPLICATE KEY UPDATE）、`int deleteByTenantIdAndDocumentId(@Param("tenantId") String, @Param("documentId") String)`、`KnowledgeDocument selectByTenantIdAndDocumentId(...)`

- [ ] **Step 1: 写失败测试（Mapper 层 upsert/delete + Controller 层 5 头鉴权）**

`InternalKnowledgeDocumentControllerTest.java`：

```java
@SpringBootTest
@AutoConfigureMockMvc
@Transactional
class InternalKnowledgeDocumentControllerTest {
    @Autowired private MockMvc mockMvc;
    @Autowired private KnowledgeDocumentMapper mapper;

    @Test
    void upsertCreatesDocument() throws Exception {
        mockMvc.perform(withInternalHeaders(post("/internal/knowledge-documents"))
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("""
                            {"document_id":"doc-001","title":"Test Doc","doc_type":"policy",
                             "business_domain":"refund","permission_group":"public",
                             "status":"enabled","source_file_name":"doc-001.md",
                             "chunk_count":3,"updated_by":"U1001"}
                            """))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.success").value(true))
                .andExpect(jsonPath("$.data.document_id").value("doc-001"))
                .andExpect(jsonPath("$.data.chunk_count").value(3));
    }

    @Test
    void upsertUpdatesExistingDocument() throws Exception {
        // 先 POST 一次再 POST 同 document_id 改 chunk_count → data.chunk_count 更新
    }

    @Test
    void deleteRemovesDocument() throws Exception {
        // 先 POST 创建 → DELETE /internal/knowledge-documents/doc-001 → success true
        // mapper.selectByTenantIdAndDocumentId("default", "doc-001") == null
    }

    @Test
    void rejectsMissingInternalToken() throws Exception {
        mockMvc.perform(post("/internal/knowledge-documents")
                        .header(TraceHeaders.TRACE_ID, TRACE_ID)
                        .header(TraceHeaders.CALLER, "ai-service")
                        .header(TraceHeaders.USER_ID, "U1001")
                        .header(TraceHeaders.TENANT_ID, "default")
                        .contentType(MediaType.APPLICATION_JSON)
                        .content("{}"))
                .andExpect(status().isUnauthorized())
                .andExpect(jsonPath("$.code").value("INTERNAL_AUTH_FAILED"));
    }
}
```

> 参考 `InternalOrderControllerTest.java` 的 import 与 `InternalApiTestSupport` 用法。`InternalRequestContext` 是否暴露 `tenantId()` 需对照 `common/security/InternalRequestResolver.java` 确认（该上下文含 caller/userId/tenantId/traceId）。

- [ ] **Step 2: 运行确认失败**

Run: `mvn test -q -Dtest=InternalKnowledgeDocumentControllerTest`
Expected: FAIL（编译失败——类不存在）

- [ ] **Step 3: Mapper 增加 3 方法**

```java
KnowledgeDocument selectByTenantIdAndDocumentId(
        @Param("tenantId") String tenantId,
        @Param("documentId") String documentId);

int upsert(KnowledgeDocument entity);

int deleteByTenantIdAndDocumentId(
        @Param("tenantId") String tenantId,
        @Param("documentId") String documentId);
```

XML 追加（注意 `knowledge_documents` 表无 `updated_by` 外键约束，字段与实体一致）：

```xml
<insert id="upsert">
    INSERT INTO knowledge_documents (
        tenant_id, document_id, title, doc_type, business_domain,
        permission_group, status, source_file_name, chunk_count,
        updated_by, created_at, updated_at
    ) VALUES (
        #{tenantId}, #{documentId}, #{title}, #{docType}, #{businessDomain},
        #{permissionGroup}, #{status}, #{sourceFileName}, #{chunkCount},
        #{updatedBy}, NOW(6), NOW(6)
    )
    ON DUPLICATE KEY UPDATE
        title = VALUES(title),
        doc_type = VALUES(doc_type),
        business_domain = VALUES(business_domain),
        permission_group = VALUES(permission_group),
        status = VALUES(status),
        source_file_name = VALUES(source_file_name),
        chunk_count = VALUES(chunk_count),
        updated_by = VALUES(updated_by),
        updated_at = NOW(6)
</insert>

<delete id="deleteByTenantIdAndDocumentId">
    DELETE FROM knowledge_documents
    WHERE tenant_id = #{tenantId} AND document_id = #{documentId}
</delete>

<select id="selectByTenantIdAndDocumentId" resultMap="KnowledgeDocumentResultMap">
    SELECT <include refid="KnowledgeDocumentColumns"/>
    FROM knowledge_documents
    WHERE tenant_id = #{tenantId} AND document_id = #{documentId}
</select>
```

- [ ] **Step 4: DTO**

```java
public record KnowledgeDocumentWriteRequest(
        String documentId,
        String title,
        String docType,
        String businessDomain,
        String permissionGroup,
        String status,
        String sourceFileName,
        int chunkCount,
        String updatedBy
) {}
```

- [ ] **Step 5: Service 接口 + 实现**

```java
KnowledgeDocumentView upsertDocument(
        InternalRequestContext context,
        KnowledgeDocumentWriteRequest request,
        String idempotencyKey);

boolean deleteDocument(InternalRequestContext context, String documentId);
```

Impl 中（校验 documentId/title 非空 → entity 组装 → mapper.upsert → 返回 `KnowledgeDocumentView.from(...)`；删除返回 `mapper.deleteByTenantIdAndDocumentId(...) > 0`）：

```java
@Override
public KnowledgeDocumentView upsertDocument(
        InternalRequestContext context,
        KnowledgeDocumentWriteRequest request,
        String idempotencyKey) {
    if (request.documentId() == null || request.documentId().isBlank()) {
        throw new BusinessException(BusinessErrorCode.DOCUMENT_ID_REQUIRED);
    }
    if (request.title() == null || request.title().isBlank()) {
        throw new BusinessException(BusinessErrorCode.DOCUMENT_TITLE_REQUIRED);
    }
    KnowledgeDocument entity = new KnowledgeDocument();
    entity.setTenantId(context.tenantId());
    entity.setDocumentId(request.documentId());
    entity.setTitle(request.title());
    entity.setDocType(request.docType() == null ? "policy" : request.docType());
    entity.setBusinessDomain(request.businessDomain() == null ? "general" : request.businessDomain());
    entity.setPermissionGroup(request.permissionGroup() == null ? "public" : request.permissionGroup());
    entity.setStatus(request.status() == null ? "enabled" : request.status());
    entity.setSourceFileName(request.sourceFileName());
    entity.setChunkCount(request.chunkCount());
    entity.setUpdatedBy(request.updatedBy() == null ? context.userId() : request.updatedBy());
    mapper.upsert(entity);
    return KnowledgeDocumentView.from(entity);
}

@Override
public boolean deleteDocument(InternalRequestContext context, String documentId) {
    return mapper.deleteByTenantIdAndDocumentId(context.tenantId(), documentId) > 0;
}
```

> 需要新增 `BusinessErrorCode.DOCUMENT_ID_REQUIRED` / `DOCUMENT_TITLE_REQUIRED`（查 `exception/BusinessErrorCode.java` 现有枚举追加）。若 `InternalRequestContext` 无 `tenantId()`/`userId()` 访问器，用实际访问器名。

- [ ] **Step 6: Controller**

```java
@RestController
@RequestMapping("/internal/knowledge-documents")
public class InternalKnowledgeDocumentController {
    private final InternalRequestResolver requestResolver;
    private final KnowledgeDocumentService knowledgeDocumentService;

    public InternalKnowledgeDocumentController(
            InternalRequestResolver requestResolver,
            KnowledgeDocumentService knowledgeDocumentService) {
        this.requestResolver = requestResolver;
        this.knowledgeDocumentService = knowledgeDocumentService;
    }

    @PostMapping
    public ApiResponse<KnowledgeDocumentView> upsert(
            @RequestBody KnowledgeDocumentWriteRequest body,
            @RequestHeader(value = TraceHeaders.IDEMPOTENCY_KEY, required = false) String idempotencyKey,
            HttpServletRequest request) {
        InternalRequestContext context = requestResolver.resolve(request);
        return ApiResponse.ok(
                knowledgeDocumentService.upsertDocument(context, body, idempotencyKey),
                context.traceId());
    }

    @DeleteMapping("/{documentId}")
    public ApiResponse<Boolean> delete(
            @PathVariable String documentId,
            HttpServletRequest request) {
        InternalRequestContext context = requestResolver.resolve(request);
        return ApiResponse.ok(
                knowledgeDocumentService.deleteDocument(context, documentId),
                context.traceId());
    }
}
```

> `ApiResponse.ok(data, traceId)` 与 `InternalOrderController` 用法一致。`InternalRequestContext.traceId()` 确认存在（InternalOrderController 已用）。

- [ ] **Step 7: 运行 Java 测试**

Run: `mvn test -q`
Expected: 基线 **80 + 新增 4 = 84 passed**

- [ ] **Step 8: Commit**

```bash
git add projects/java-business-service/
git commit -m "feat: add internal knowledge-document upsert/delete endpoints"
```

---

### Task 4: Python KnowledgeDocumentClient + 文档 CRUD API

**Files:**
- Create: `projects/ai-service/app/services/java_knowledge_document_client.py`（仿 `java_order_client.py`）
- Modify: `projects/ai-service/app/routers/knowledge_base.py`（+5 端点：GET list / POST create / PUT update / DELETE / POST ingest）
- Modify: `projects/ai-service/app/schemas/knowledge_base.py`（+请求/响应 schema）
- Modify: `projects/ai-service/app/rag/ingestion.py`（新增单文档 ingest/update helper，或复用现有函数——见下）
- Test: `projects/ai-service/tests/test_knowledge_document_crud_api.py`

**Interfaces:**
- Consumes: `JavaOrderClient` 模式（`from_settings` / `_build_headers` / `_unwrap_java_api_response_data`）；`java_knowledge_document_client` 提供：
  - `KnowledgeDocumentClient.from_settings(settings) -> KnowledgeDocumentClient`
  - `upsert_document(payload: dict) -> dict`（POST `/internal/knowledge-documents`）
  - `delete_document(document_id: str) -> bool`（DELETE `/internal/knowledge-documents/{id}`）
- Consumes: `load_document(path)`（loaders.py:66）、`split_documents_into_chunks`（splitters.py）、`embed_chunks`（embeddings.py）、`QdrantVectorStore`（Task 1 多库）、`delete_document_from_vector_store`（ingestion.py:141）
- Produces:
  - `GET /api/knowledge-base/documents` → `KnowledgeBaseDocumentListView`（文档列表：本地文件元数据 + Java 元数据 + collection + chunk 数）
  - `POST /api/knowledge-base/documents`（body：`KnowledgeBaseDocumentCreateRequest`）→ `KnowledgeBaseDocumentView`（落盘 + Java upsert + Qdrant 同步）
  - `PUT /api/knowledge-base/documents/{document_id}`（body：`KnowledgeBaseDocumentUpdateRequest`）→ 重新切分 upsert 删旧 chunk
  - `DELETE /api/knowledge-base/documents/{document_id}` → 删文件 + Java delete + 删 Qdrant chunk
  - `POST /api/knowledge-base/documents/{document_id}/ingest`（body：`KnowledgeBaseDocumentIngestRequest`）→ 单独同步
  - 请求 schema 字段：`document_id` / `title` / `content` / `business_domain` / `permission_group` / `doc_type` / `collection_name` / `embedding_mode` / `chunk_size` / `chunk_overlap`

- [ ] **Step 1: 写失败测试（CRUD 全流程，fake client + 临时目录）**

`test_knowledge_document_crud_api.py`：

```python
def test_create_document_writes_file_and_syncs(client, tmp_path, monkeypatch) -> None:
    # monkeypatch settings.knowledge_base_directory → tmp_path
    # monkeypatch KnowledgeDocumentClient → fake（记录 upsert 调用）
    # monkeypatch QdrantVectorStore → fake（记录 upsert/delete）
    response = client.post(
        "/api/knowledge-base/documents",
        json={
            "document_id": "doc-001",
            "title": "Test Policy",
            "content": "# Test Policy\n\n退款政策七天无理由。",
            "business_domain": "refund",
            "permission_group": "public",
            "collection_name": "kb_customer_policy",
        },
    )
    assert response.status_code == 200
    data = response.json()["data"]
    assert data["document_id"] == "doc-001"
    # 文件已落盘
    assert (tmp_path / "doc-001.md").exists()
    # fake upsert 被调用


def test_update_document_resyncs_and_deletes_old_chunks(client, tmp_path, monkeypatch) -> None:
    # 先 create → 再 PUT 改 content → 断言 fake delete_document_from_vector_store 被调用 + 新 upsert


def test_delete_document_removes_file_and_metadata(client, tmp_path, monkeypatch) -> None:
    # 先 create → DELETE → 文件不存在 + fake delete_document + fake java delete 被调用


def test_list_documents_merges_local_and_java_metadata(client, tmp_path, monkeypatch) -> None:
    # 本地放一个文件 + fake java list → 返回合并列表
```

> 参考现有 `test_knowledge_base_api.py` 的 client fixture 与 monkeypatch 风格。Qdrant fake 实现 `ensure_collection` / `upsert_embedded_chunks` / `delete_points_by_filter`。

- [ ] **Step 2: 运行确认失败**

Run: `uv run pytest tests/test_knowledge_document_crud_api.py -v`
Expected: FAIL（404）

- [ ] **Step 3: 实现 client**

```python
class KnowledgeDocumentClient:
    def __init__(self, *, base_url, timeout_seconds, settings) -> None:
        self._base_url = base_url
        self._timeout_seconds = timeout_seconds
        self._settings = settings

    @classmethod
    def from_settings(cls, settings) -> "KnowledgeDocumentClient":
        return cls(
            base_url=settings.resolved_java_business_service_base_url,
            timeout_seconds=settings.resolved_java_business_service_timeout_seconds,
            settings=settings,
        )

    def upsert_document(self, payload: dict) -> dict:
        with httpx.Client(base_url=self._base_url, timeout=self._timeout_seconds) as client:
            response = client.post(
                "/internal/knowledge-documents",
                json=payload,
                headers=self._build_headers(),
            )
        return _unwrap_java_api_response_data(response)

    def delete_document(self, document_id: str) -> bool:
        with httpx.Client(base_url=self._base_url, timeout=self._timeout_seconds) as client:
            response = client.delete(
                f"/internal/knowledge-documents/{document_id}",
                headers=self._build_headers(),
            )
        return bool(_unwrap_java_api_response_data(response))

    def _build_headers(self) -> dict:
        headers = build_java_internal_headers(self._settings)
        headers.update(build_trace_headers())
        return headers
```

> 完全照抄 `java_order_client.py` 的 import 与错误处理（`build_java_internal_headers` 在 `core/business_context.py`；`_unwrap_java_api_response_data` 在 `java_order_client.py` 可复用 import）。

- [ ] **Step 4: 实现 schema**

```python
class KnowledgeBaseDocumentCreateRequest(BaseModel):
    document_id: str = Field(min_length=1, max_length=64)
    title: str = Field(min_length=1, max_length=200)
    content: str = Field(min_length=1)
    business_domain: str = Field(default="general", max_length=64)
    permission_group: str = Field(default="public", max_length=64)
    doc_type: str = Field(default="policy", max_length=32)
    collection_name: str = Field(min_length=1, max_length=128)
    embedding_mode: KnowledgeBaseEmbeddingMode = Field(default="fake")
    chunk_size: int = Field(default=500, ge=100, le=4000)
    chunk_overlap: int = Field(default=80, ge=0, le=1000)


class KnowledgeBaseDocumentUpdateRequest(BaseModel):
    title: str | None = Field(default=None, max_length=200)
    content: str | None = None
    business_domain: str | None = Field(default=None, max_length=64)
    permission_group: str | None = Field(default=None, max_length=64)
    doc_type: str | None = Field(default=None, max_length=32)
    embedding_mode: KnowledgeBaseEmbeddingMode = Field(default="fake")
    chunk_size: int = Field(default=500, ge=100, le=4000)
    chunk_overlap: int = Field(default=80, ge=0, le=1000)


class KnowledgeBaseDocumentIngestRequest(BaseModel):
    embedding_mode: KnowledgeBaseEmbeddingMode = Field(default="fake")
    chunk_size: int = Field(default=500, ge=100, le=4000)
    chunk_overlap: int = Field(default=80, ge=0, le=1000)


class KnowledgeBaseDocumentView(BaseModel):
    document_id: str = Field(min_length=1)
    title: str = Field(min_length=1)
    business_domain: str
    permission_group: str
    doc_type: str
    collection_name: str
    chunk_count: int = Field(default=0, ge=0)
    source_file_name: str
    exists_local: bool = False
    status: str = "enabled"
    updated_at: str | None = None


class KnowledgeBaseDocumentListView(BaseModel):
    documents: list[KnowledgeBaseDocumentView] = Field(default_factory=list)
    document_count: int = Field(ge=0)
    trace_id: str = Field(min_length=1)
```

- [ ] **Step 5: 实现单文档 ingest/update helper（ingestion.py）**

```python
def ingest_single_document(
    path: Path | str,
    *,
    embedding_model: EmbeddingModel,
    vector_store: VectorStoreWriter,
    chunk_size: int = DEFAULT_CHUNK_SIZE,
    chunk_overlap: int = DEFAULT_CHUNK_OVERLAP,
    wait: bool = True,
) -> RagIngestionResult:
    document = load_document(path)
    chunks = split_documents_into_chunks([document], chunk_size=chunk_size, chunk_overlap=chunk_overlap)
    embedded_chunks = embed_chunks(chunks, embedding_model=embedding_model)
    vector_store.ensure_collection(vector_size=embedding_model.dimension, distance="Cosine")
    vector_count = vector_store.upsert_embedded_chunks(embedded_chunks, wait=wait)
    return RagIngestionResult(
        document_count=1,
        chunk_count=len(chunks),
        vector_count=vector_count,
        vector_dimension=embedding_model.dimension,
        collection_name=vector_store.collection_name,
    )


def update_single_document(
    path: Path | str,
    *,
    embedding_model: EmbeddingModel,
    vector_store: VectorStoreUpdater,
    chunk_size: int = DEFAULT_CHUNK_SIZE,
    chunk_overlap: int = DEFAULT_CHUNK_OVERLAP,
    wait: bool = True,
) -> RagIngestionResult:
    source = path.name if isinstance(path, Path) else Path(path).name
    delete_document_from_vector_store(source, vector_store=vector_store, wait=wait)
    return ingest_single_document(
        path,
        embedding_model=embedding_model,
        vector_store=vector_store,
        chunk_size=chunk_size,
        chunk_overlap=chunk_overlap,
        wait=wait,
    )
```

> 注意：`load_document` 的 source 是**相对 base_dir 的文件名**（`_build_source`）。单文档路径直接传文件名作为 source，保证 delete filter 与 upsert payload 一致。

- [ ] **Step 6: 实现 5 个端点（router）**

```python
@router.get("/documents", response_model=KnowledgeBaseDocumentListView)
def list_documents(
    settings: Settings = Depends(get_settings),
    trace: TraceContext = Depends(get_trace_context),
) -> KnowledgeBaseDocumentListView:
    directory = Path(settings.knowledge_base_directory)
    local = load_documents_from_directory(directory)
    java_docs = _java_client(settings).list_documents()
    # merge：本地 source_file_name ↔ java document_id
    ...


@router.post("/documents", response_model=KnowledgeBaseDocumentView)
def create_document(
    body: KnowledgeBaseDocumentCreateRequest,
    settings: Settings = Depends(get_settings),
    trace: TraceContext = Depends(get_trace_context),
) -> KnowledgeBaseDocumentView:
    directory = Path(settings.knowledge_base_directory)
    file_path = directory / f"{body.document_id}.md"
    content = _render_markdown(body.title, body.content, body.business_domain, body.permission_group)
    file_path.write_text(content, encoding="utf-8")
    # 同步 Qdrant
    store = _build_vector_store(settings, collection_name=body.collection_name)
    result = ingest_single_document(
        file_path,
        embedding_model=_build_embedding_model(settings, body.embedding_mode),
        vector_store=store,
        chunk_size=body.chunk_size,
        chunk_overlap=body.chunk_overlap,
    )
    # Java 元数据 upsert
    _java_client(settings).upsert_document({
        "document_id": body.document_id,
        "title": body.title,
        "doc_type": body.doc_type,
        "business_domain": body.business_domain,
        "permission_group": body.permission_group,
        "status": "enabled",
        "source_file_name": file_path.name,
        "chunk_count": result.chunk_count,
        "updated_by": "ai-service",
    })
    return KnowledgeBaseDocumentView(...)
```

> PUT/DELETE/ingest 端点照此模式。`_java_client(settings)` / `_build_embedding_model(settings, mode)` 定义为 router 模块级工厂（`_build_embedding_model` 从 `app.rag.embeddings` 构造 fake/real，参考现有 ingest 端点如何构造 embedding model——查 `knowledge_base.py` 现有 ingest 实现）。`_render_markdown` 生成带行内元数据的 markdown（`<!-- doc_type: policy / business_domain: refund / permission_group: public -->`，与 loaders 解析格式一致——查 `loaders.py` 的 `_extract_inline_metadata` 确认注释格式）。

- [ ] **Step 7: 运行测试**

Run: `uv run pytest tests/test_knowledge_document_crud_api.py -q` → 全过
Run: `uv run pytest -q` → **1564 passed**

- [ ] **Step 8: Commit**

```bash
git add projects/ai-service/
git commit -m "feat: add knowledge-document CRUD api with java metadata sync"
```

---

### Task 5: 前端 KnowledgeBaseView 扩展（上传/编辑/删除/单独同步）

**Files:**
- Modify: `projects/customer-service-console/src/services/knowledgeBaseApi.ts`（+6 方法）
- Modify: `projects/customer-service-console/src/views/KnowledgeBaseView.vue`（+操作列、上传/编辑弹窗、删除确认、collection 状态展示）

**Interfaces:**
- Consumes: 后端端点（Task 2 / Task 4）：`GET /api/knowledge-base/collections`、`GET /api/knowledge-base/documents`、`POST /api/knowledge-base/documents`、`PUT /api/knowledge-base/documents/{id}`、`DELETE /api/knowledge-base/documents/{id}`、`POST /api/knowledge-base/documents/{id}/ingest`
- Produces: 前端交互（文档表格操作列 + 上传表单弹窗 + 编辑弹窗 + 删除确认 + collection 状态标签）

- [ ] **Step 1: 写前端 API 方法（先改 ts，保证 build 可编译）**

```typescript
export interface KnowledgeBaseCollectionStatus {
  collection_name: string
  knowledge_base_ids: string[]
  display_name: string
  point_count: number
  exists: boolean
  is_legacy: boolean
}

export interface KnowledgeBaseCollectionsResponse {
  collections: KnowledgeBaseCollectionStatus[]
  legacy_collections: KnowledgeBaseCollectionStatus[]
  trace_id: string
}

export interface KnowledgeBaseDocumentItem {
  document_id: string
  title: string
  business_domain: string
  permission_group: string
  doc_type: string
  collection_name: string
  chunk_count: number
  source_file_name: string
  exists_local: boolean
  status: string
  updated_at: string | null
}

export interface KnowledgeBaseDocumentCreateRequest {
  document_id: string
  title: string
  content: string
  business_domain: string
  permission_group: string
  doc_type: string
  collection_name: string
  embedding_mode: KnowledgeBaseEmbeddingMode
  chunk_size: number
  chunk_overlap: number
}

export async function getKnowledgeBaseCollections() {
  const response = await aiApi.get<KnowledgeBaseCollectionsResponse>('/api/knowledge-base/collections')
  return response.data
}

export async function listKnowledgeBaseDocuments() {
  const response = await aiApi.get<{ documents: KnowledgeBaseDocumentItem[]; document_count: number }>('/api/knowledge-base/documents')
  return response.data
}

export async function createKnowledgeBaseDocument(request: KnowledgeBaseDocumentCreateRequest) {
  const response = await aiApi.post<KnowledgeBaseDocumentItem>('/api/knowledge-base/documents', request)
  return response.data
}

export async function updateKnowledgeBaseDocument(documentId: string, request: Partial<KnowledgeBaseDocumentCreateRequest>) {
  const response = await aiApi.put<KnowledgeBaseDocumentItem>(`/api/knowledge-base/documents/${documentId}`, request)
  return response.data
}

export async function deleteKnowledgeBaseDocument(documentId: string) {
  const response = await aiApi.delete<{ success: boolean }>(`/api/knowledge-base/documents/${documentId}`)
  return response.data
}

export async function ingestKnowledgeBaseDocument(documentId: string, request: { embedding_mode: KnowledgeBaseEmbeddingMode; chunk_size: number; chunk_overlap: number }) {
  const response = await aiApi.post<KnowledgeBaseDocumentItem>(`/api/knowledge-base/documents/${documentId}/ingest`, request)
  return response.data
}
```

> 复用现有 `buildAiServiceError` helper（knowledgeBaseApi.ts 已有）。

- [ ] **Step 2: 运行 build 确认类型编译**

Run: `npm run build` → 通过（API 未使用不报错）

- [ ] **Step 3: 扩展 KnowledgeBaseView.vue**

- 模板：文档表格（`document-table` 现有表格基础上）加操作列：`编辑` / `删除` / `同步` 三个按钮；页面上方加"上传文档"按钮；collection 状态区展示 kb_* collection 名称 + exists 标签 + point_count
- 逻辑：`loadDocuments()` / `loadCollections()`；`openCreateDialog()` / `openEditDialog(row)` / `confirmDelete(row)` / `runSingleIngest(row)`；提交后刷新两个列表
- 弹窗：创建/编辑共用表单（title / document_id（仅创建）/ business_domain / permission_group / doc_type / collection_name（select 自 collections）/ content textarea）
- 沿用现有 `el-table` / `el-dialog` / `el-form` / `ElMessage` 风格（查 KnowledgeBaseView.vue 现有 import 与用法）

- [ ] **Step 4: 运行 build 确认**

Run: `npm run build` → 通过

- [ ] **Step 5: Commit**

```bash
git add projects/customer-service-console/
git commit -m "feat: add knowledge-document management to knowledge base view"
```

---

### Task 6: 前端趋势图 RAG 检索质量线

**Files:**
- Modify: `projects/customer-service-console/src/views/EvaluationView.vue`（+rag 序列 computed + 第三条 series + legend + tooltip 文案）

**Interfaces:**
- Consumes: `getEvaluationHistory()` 返回的 `trendHistory.value`（已有 `agent_eval` / `production_regression`，**且 history 端点已返回 `rag_retrieval`**——evaluation.py:389-404 已实现，每点含 `hit_rate` / `recall` / `mrr`）
- Produces: 趋势图第三条线"RAG 检索命中率"（hit_rate）

- [ ] **Step 1: 加 rag 序列 computed**

```typescript
const trendRagPoints = computed<[number, number][]>(() =>
  (trendHistory.value?.rag_retrieval || [])
    .map((point) => {
      if (!point.started_at || point.hit_rate == null) {
        return null
      }
      return [new Date(point.started_at).getTime(), point.hit_rate] as [number, number]
    })
    .filter((point): point is [number, number] => point !== null)
    .sort((left, right) => left[0] - right[0]),
)

const trendEmpty = computed(
  () =>
    trendAgentPoints.value.length === 0 &&
    trendRegressionPoints.value.length === 0 &&
    trendRagPoints.value.length === 0,
)
```

- [ ] **Step 2: 加 series 与 legend**

```typescript
legend: { data: ['Agent 通过率', '回归通过率', 'RAG 检索命中率'] },
// series 追加：
{
  name: 'RAG 检索命中率',
  type: 'line',
  smooth: true,
  showSymbol: false,
  lineStyle: { color: '#E6A23C', width: 2 },
  itemStyle: { color: '#E6A23C' },
  data: trendRagPoints.value,
},
```

- [ ] **Step 3: tooltip 文案区分（可选，现有 formatter 显示"通过率"，RAG 线显示"命中率"）**

```typescript
formatter: (params: unknown) => {
  const items = (Array.isArray(params) ? params : [params]) as Array<{
    marker: string
    seriesName: string
    value: [number, number]
  }>
  return items
    .map((item) => {
      const [timestamp, value] = item.value
      const time = new Date(timestamp).toLocaleString('zh-CN', { hour12: false })
      const label = item.seriesName.includes('RAG') ? '命中率' : '通过率'
      return `${item.marker}${item.seriesName}（${time}）<br/>${label} ${(value * 100).toFixed(1)}%`
    })
    .join('<br/>')
},
```

- [ ] **Step 4: 运行 build**

Run: `npm run build` → 通过

- [ ] **Step 5: Commit**

```bash
git add projects/customer-service-console/
git commit -m "feat: add rag retrieval quality line to evaluation trend chart"
```

---

### Task 7: 全量回归 + 真实验收 + 文档更新

**Files:**
- Modify: `docs/project-handoff-for-vibe-coding.md`（更新阶段 3 完成状态）
- Modify: `docs/superpowers/specs/2026-08-07-rag-enhancement-design.md`（标记阶段 3 已实现，可选）
- 临时：`projects/ai-service/data/knowledge_base/` 上传测试文档（验收后清理或保留）

**Interfaces:**
- Consumes: 全部 Task 1-6 产物

- [ ] **Step 1: 三套全量回归**

Run: `cd projects/ai-service && uv run pytest -q` → **1564 passed**
Run: `cd projects/java-business-service && mvn test -q` → **84 passed**
Run: `cd projects/customer-service-console && npm run build` → ✓

- [ ] **Step 2: 真实验收（Python 8000 + VM Qdrant + Java 18004）**

1. 启动 Java（`mvn spring-boot:run`）与 Python（标准 uvicorn）。
2. `POST /api/knowledge-base/documents` 上传新文档（`document_id="phase3-accept-001"`，`collection_name="kb_customer_policy"`，内容含"运费险赔付"）。
3. `GET /api/knowledge-base/collections` → kb_customer_policy exists=true、point_count>0。
4. 对话问"运费险怎么赔" → 回复引用 phase3-accept-001 内容。
5. `PUT /api/knowledge-base/documents/phase3-accept-001` 改内容 → 再问 → 新内容生效。
6. `DELETE /api/knowledge-base/documents/phase3-accept-001` → 再问 → 不再引用该文档。
7. 前端 KnowledgeBaseView 上传/编辑/删除/同步按钮可用；EvaluationView 趋势图显示三条线。

- [ ] **Step 3: 更新交接文档**

`docs/project-handoff-for-vibe-coding.md` 阶段 3 状态 → 完成；记录新端点与 collection 布局。

- [ ] **Step 4: Commit**

```bash
git add docs/ projects/ai-service/data/knowledge_base/
git commit -m "docs: mark rag phase 3 complete"
```

---

## Self-Review

**1. Spec coverage:**
- 5.1 多 collection：Task 1（from_settings 多库 + list/count）+ Task 2（collection 管理端点）✅
- 5.2 文档 CRUD + Java 持久化：Task 3（Java 写接口）+ Task 4（Python CRUD + client）✅
- 5.3 前端 KnowledgeBaseView：Task 5 ✅
- 5.4 趋势图扩展：Task 6（后端 history 已含 rag_retrieval，前端补线）✅
- 6 测试与验收：Task 7 全量回归 + 真实验收 ✅
- 8 开放点：多 collection 决策（用户选新建+旧库兼容）→ Task 2 legacy 展示；Java/Python 协作（用户选内部接口）→ Task 3+4 ✅

**2. Placeholder scan:** 无 TBD/TODO；所有代码块完整。

**3. Type consistency:**
- `QdrantVectorStore.from_settings(settings, *, collection_name=None)` 在 Task 1 定义，Task 2/4 复用 ✅
- `KnowledgeDocumentClient.upsert_document/delete_document` Task 4 定义，Task 4 端点复用 ✅
- Java `KnowledgeDocumentWriteRequest` / `upsertDocument` / `deleteDocument` Task 3 定义，测试同任务验证 ✅
- 前端 API 方法与后端端点路径一致（Task 4 → Task 5）✅
- `RagIngestionResult` 已有 `chunk_count` 字段（ingestion.py 现有）✅

**已知风险（非阻塞）：**
- `InternalRequestContext` 的 `tenantId()`/`userId()`/`traceId()` 访问器名以实际代码为准（InternalOrderController 已用 `context.traceId()`）。
- `_render_markdown` 的行内元数据注释格式需对照 `loaders.py` 的 `_extract_inline_metadata`。
- Task 4 的 `_build_embedding_model(settings, mode)` 参考现有 ingest 端点构造方式。
