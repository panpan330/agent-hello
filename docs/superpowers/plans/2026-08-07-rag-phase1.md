# RAG 体系完善·阶段 1（A 收尾 + C 双模式评测）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 RAG 体系完善的阶段 1——A 对话链路真实化收尾（ProductionPolicyRagService 补权限过滤 + Fake 标 deprecated）+ C 真实检索评测（评测脚本双模式 keyword/vector + 结果入库）。

**Architecture:** A 是两处小改（补 retrieve_top_k 权限过滤参数 + deprecated 注释）；C 是评测脚本扩展（`--retriever=keyword|vector` 双模式，vector 连真 Qdrant+真 embedding）+ 结果入库（新建 rag_retrieval_runs.json 原子写 + history 端点加 rag_retrieval 序列）。阶段 2（B 高级模块）依赖本阶段的 vector 评测模式与 runs 存储模式。

**Tech Stack:** Python 3.12 + FastAPI + Pydantic + Qdrant；pytest。

## Global Constraints

- 自动测试不调用真实模型、不命中真实 Embedding/Rerank API、不写入真实业务数据、不依赖真实 Redis。
- 现有测试套件保持绿色：Python `uv run pytest -q` = 1524 passed；前端 `npm run build` 通过。
- 不新增第三方依赖。
- 评测脚本 `--retriever=keyword|vector`（默认 keyword 保测试快）；vector 模式连真 Qdrant + 真 embedding（手动冒烟，非自动测试）。
- runs 入库原子写（.tmp + replace，仿 production_regression_history.py:27-39）；保留最近 30 条。
- `ProductionPolicyRagService` 补权限过滤参数（对话场景默认不限）；`FakePolicyRagService` 标 deprecated 不改行为。
- 本地 git commit；不推送 GitHub。

---

### Task 1: A 收尾——ProductionPolicyRagService 补权限过滤 + Fake 标 deprecated

**Files:**
- Modify: `projects/ai-service/app/services/console_agent_service.py`（ProductionPolicyRagService :211-251）
- Modify: `projects/ai-service/app/agents/ticket_agent.py`（create_policy_rag_service :3640）
- Test: `projects/ai-service/tests/test_console_agent_service.py`（或相关）

**Interfaces:**
- Consumes: `retrieve_top_k`（retriever.py:31-43，含 permission_group/business_domain/doc_type/source 参数）
- Produces: `ProductionPolicyRagService.answer_policy_question(query, *, access_scope=None)` 传权限过滤参数（阶段 2 的 enhanced pipeline 复用此签名）

- [ ] **Step 1: 写失败测试**

```python
# tests/test_console_agent_service.py 追加
def test_production_policy_rag_service_passes_permission_filters():
    # monkeypatch retrieve_top_k 捕获参数 → 调 answer_policy_question
    # 断言 permission_group/business_domain/doc_type/source 被传入（默认值）
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd projects/ai-service && uv run pytest tests/test_console_agent_service.py -q`
Expected: 新测试失败（retrieve_top_k 未传 filter 参数）

- [ ] **Step 3: ProductionPolicyRagService 补传参**

`answer_policy_question` 中 `retrieve_top_k(...)` 补：
```python
retrieved_chunks = retrieve_top_k(
    query,
    embedding_model=embedding_model,
    vector_store=QdrantVectorStore.from_settings(self.settings),
    top_k=self.settings.rerank_candidate_count,
    # 对话场景默认不限权限（None 语义）；阶段 2 enhanced pipeline 可传真实 scope
    access_scope=None,
    permission_group=None,
    business_domain=None,
    doc_type=None,
    source=None,
)
```
（跟随 ask_rag 端点 rag.py:85-95 的传参模式——它已传 permission_group 等）

- [ ] **Step 4: Fake 标 deprecated**

`ticket_agent.py:3640` `create_policy_rag_service` 加 docstring：
```python
def create_policy_rag_service() -> PolicyRagService:
    """⚠️ deprecated: 仅测试/兜底使用（FakePolicyRagService）。

    生产对话链路由 ConsoleAgentService 注入 ProductionPolicyRagService（真实 RAG）；
    此函数仅在被显式注入 None 时兜底，新代码不应依赖。
    """
    return FakePolicyRagService()
```

- [ ] **Step 5: 跑测试确认通过 + 相关套件**

Run: `cd projects/ai-service && uv run pytest tests/test_console_agent_service.py -q`
Expected: 新测试 + 原有全过

- [ ] **Step 6: 全量 pytest + Commit**

Run: `cd projects/ai-service && uv run pytest -q`
Expected: ≥1524 passed
```bash
git add projects/ai-service/
git commit -m "feat: pass permission filters in production rag service and mark fake deprecated"
```

---

### Task 2: C 评测脚本双模式（--retriever=keyword|vector）

**Files:**
- Modify: `projects/ai-service/scripts/rag_retrieval_eval.py`
- Test: `projects/ai-service/tests/test_rag_retrieval_eval.py`（或相关，看现有测试是否覆盖脚本）

**Interfaces:**
- Consumes: `retrieve_top_k`（retriever.py:31-43）、`OpenAICompatibleEmbeddingModel.from_settings`（embeddings.py:83-104）、`QdrantVectorStore.from_settings`（vector_store.py:73-80）、`evaluate_retrieval_results`（evaluation.py:784-821）
- Produces: `main()` 支持 `--retriever=keyword|vector`；vector 模式返回 `list[RetrievedChunk]`（已兼容 evaluate_retrieval_results）

- [ ] **Step 1: 写失败测试**

```python
# tests/ 下脚本相关测试（若存在 test_rag_retrieval_eval.py 则追加；否则新建）
def test_script_parses_retriever_argument():
    # 构造 argv --retriever=vector → parser 接受；默认 keyword
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 脚本加 --retriever 参数 + vector 分支**

```python
parser.add_argument("--retriever", choices=["keyword", "vector"], default="keyword")

if args.retriever == "keyword":
    documents = load_documents_from_directory(KNOWLEDGE_BASE_DIR)
    chunks = split_documents_into_chunks(documents)
    keyword_retriever = SimpleKeywordRetriever(chunks)
    # 现有逻辑（search → _keyword_result_to_retrieved_chunk）
else:  # vector
    from app.core.config import get_settings
    from app.rag.embeddings import OpenAICompatibleEmbeddingModel
    from app.rag.retriever import retrieve_top_k
    from app.rag.vector_store import QdrantVectorStore
    settings = get_settings()
    for eval_case in cases:
        retrievals_by_case_id[eval_case.id] = retrieve_top_k(
            eval_case.query,
            embedding_model=OpenAICompatibleEmbeddingModel.from_settings(settings),
            vector_store=QdrantVectorStore.from_settings(settings),
            top_k=args.top_k,
            permission_group=eval_case.permission_group,
            business_domain=eval_case.business_domain,
            doc_type=eval_case.doc_type,
            source=eval_case.source,
        )
```

- [ ] **Step 4: 跑测试确认通过 + keyword 模式冒烟（保现有行为）**

Run: `cd projects/ai-service && uv run python scripts/rag_retrieval_eval.py --retriever=keyword`
Expected: 输出 Hit@K/Recall@K 摘要（与改造前一致）

- [ ] **Step 5: Commit**

```bash
git add projects/ai-service/
git commit -m "feat: add vector retriever mode to rag retrieval eval script"
```

---

### Task 3: C 评测结果入库（rag_retrieval_runs.json + history 序列）

**Files:**
- Create: `projects/ai-service/app/evaluation/rag_retrieval_history.py`（runs 存储，仿 production_regression_history.py）
- Modify: `projects/ai-service/scripts/rag_retrieval_eval.py`（跑完后 append run）
- Modify: `projects/ai-service/app/routers/evaluation.py`（history 端点加 rag_retrieval 序列）
- Modify: `projects/ai-service/app/schemas/evaluation.py`（EvaluationHistoryView 加 rag_retrieval）
- Test: `projects/ai-service/tests/test_evaluation_api.py` + `tests/test_rag_retrieval_history.py`

**Interfaces:**
- Consumes: `RetrievalEvalSummary`（evaluation.py:596）、`load_retrieval_eval_cases`、`evaluate_retrieval_results`
- Produces: `RagRetrievalRun`（run_id/retriever_mode/started_at/completed_at/hit_rate/recall/precision/mrr/case_count/results[]）；`load_rag_retrieval_runs(path)`/`append_rag_retrieval_run(path, run)`；`get_rag_retrieval_history_path` 依赖；`GET /api/ai/evaluation/history` 加 `rag_retrieval` 序列 `[{started_at, hit_rate, recall, mrr}]`

- [ ] **Step 1: 写失败测试**

```python
# tests/test_rag_retrieval_history.py（新建）
def test_append_and_load_rag_retrieval_runs():
    # append 2 条 → load 返回 2 条按序；30 条上限
# tests/test_evaluation_api.py 追加
def test_history_includes_rag_retrieval_sequence():
    # 造 rag_retrieval_runs.json → GET /history → rag_retrieval 序列含 started_at/hit_rate/recall/mrr
```

- [ ] **Step 2: 跑测试确认失败**

- [ ] **Step 3: 实现 rag_retrieval_history.py**

仿 production_regression_history.py 完整模式（含 _history_lock / MAX_STORED_RUNS=30 / 原子写）：
```python
class RagRetrievalRun(BaseModel):
    run_id: str = Field(min_length=1)
    retriever_mode: Literal["keyword", "vector"]
    started_at: datetime
    completed_at: datetime
    top_k: int
    hit_rate: float
    recall: float
    precision: float
    mrr: float
    case_count: int
    results: list[dict] = Field(default_factory=list)  # 每 case {case_id, hit, recall, precision, rank}

class RagRetrievalRunHistory(BaseModel):
    schema_version: str = "stage12.rag_retrieval_runs.v1"
    runs: list[RagRetrievalRun] = Field(default_factory=list)

def load_rag_retrieval_runs(path: Path) -> list[RagRetrievalRun]: ...
def load_latest_rag_retrieval_run(path: Path) -> RagRetrievalRun | None: ...
def append_rag_retrieval_run(path: Path, run: RagRetrievalRun) -> RagRetrievalRun: ...  # 原子写 + 30 条上限
```

- [ ] **Step 4: 评测脚本 append run**

`main()` 末尾（evaluate_retrieval_results 后）：
```python
if args.save_run:  # 新增 --save-run 参数（默认 false，冒烟不落库）
    from app.evaluation.rag_retrieval_history import RagRetrievalRun, append_rag_retrieval_run
    from pathlib import Path as P
    run = RagRetrievalRun(
        run_id=f"rag-retrieval-{uuid4().hex[:12]}",
        retriever_mode=args.retriever,
        started_at=started, completed_at=datetime.now(timezone.utc),
        top_k=args.top_k,
        hit_rate=summary.hit_rate_at_k, recall=summary.recall_at_k,
        precision=summary.precision_at_k, mrr=summary.mrr_at_k,
        case_count=len(cases),
        results=[_case_result_to_dict(r) for r in summary.results],
    )
    append_rag_retrieval_run(RAG_RETRIEVAL_HISTORY_PATH, run)
```

- [ ] **Step 5: history 端点加 rag_retrieval 序列**

`routers/evaluation.py` `/history` 加第三序列（仿 production_regression 分支，读 `get_rag_retrieval_history_path` → load_rag_retrieval_runs → `[{started_at, hit_rate, recall, mrr}]`）；`schemas/evaluation.py` 的 `EvaluationHistoryView` 加 `rag_retrieval: list[...]`（默认空列表）。

- [ ] **Step 6: 跑测试确认通过 + 相关套件**

- [ ] **Step 7: 全量 pytest + Commit**

Run: `cd projects/ai-service && uv run pytest -q`
```bash
git add projects/ai-service/
git commit -m "feat: persist rag retrieval eval runs and expose in history"
```

---

### Task 4: 阶段 1 真实验收 + 文档更新

**Files:**
- Modify: `docs/project-handoff-for-vibe-coding.md`（第 17/18 节更新 RAG 能力）
- 无代码变更（验收）

**Interfaces:**
- Consumes: Task 1-3 交付

- [ ] **Step 1: 全量回归**

Run: `cd projects/ai-service && uv run pytest -q`（≥1524 passed）+ `cd projects/customer-service-console && npm run build`

- [ ] **Step 2: 真实验收（启动 Python 8000 + VM Qdrant）**

- **A 验证**：对话问"退款政策是什么" → ProductionPolicyRagService 走真 RAG（返回真实引用，非硬编码）
- **C 验证**：`uv run python scripts/rag_retrieval_eval.py --retriever=vector --save-run` → 输出 Hit@K/Recall@K 摘要 + runs.json 落库 → `GET /api/ai/evaluation/history` 含 rag_retrieval 序列（3 条序列：agent_eval/production_regression/rag_retrieval）

- [ ] **Step 3: 更新交接文档**

- 第 17 节加"RAG 体系完善·阶段 1"行（A 权限过滤 + C 双模式评测）
- 第 18 节里程碑加行

- [ ] **Step 4: 最终全分支审查 + Commit**

```bash
git add docs/project-handoff-for-vibe-coding.md
git commit -m "docs: update handoff with rag phase 1 milestone"
```

---

## Self-Review 记录

**1. Spec 覆盖：**
- 3.1 A 收尾（权限过滤 + Fake deprecated）→ Task 1 ✅
- 3.2 C 双模式评测（--retriever + 指标 + 结果入库）→ Task 2 + Task 3 ✅
- 3.2 配置 `RAG_RETRIEVAL_EVAL_RETRIEVER` → Task 2（脚本默认参数）✅
- 趋势图 rag_retrieval 扩展（必做）→ Task 3（history 序列；前端第三条线在阶段 3）——**注意：前端趋势图线放阶段 3 一并做（依赖 B 的更多序列），本阶段先出数据**

**2. 占位符扫描：** 无 TBD/TODO；Task 3 的 `_case_result_to_dict` 与 `summary.results` 结构实现时读 evaluation.py 确认（RetrievalEvalSummary.results 字段）。

**3. 类型一致性：**
- `RagRetrievalRun` 字段在 Task 3 定义、脚本 append 使用——一致 ✅
- `retrieve_top_k` 权限参数在 Task 1/2 使用同一签名 ✅
- `get_rag_retrieval_history_path` 在 Task 3 定义、history 端点使用 ✅

**开放点（实现时按实际代码修正）：**
- `RetrievalEvalSummary.results` 的每 case 结构（hit/recall/rank 字段名）——Task 3 读 evaluation.py:596-646 确认
- 评测脚本 `--save-run` 默认 false（冒烟不落库），显式开启才入库
